schema {
    query: Query
}

type Query {
    allContacts: [Contact]
    contact(id: String): Contact
}

type Contact {
    firstName: String
    lastName: String
    email: String
    phone: String
    message: String
}